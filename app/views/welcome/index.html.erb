<% if current_user %>
  <h3>Welcome: <%= current_user.email %></h3>
  <% if current_user.admin? %>
  <h4><%= link_to 'List of User', users_path %></h4>

  <% end %>
  <h5><%= link_to "Show your details", user_path(current_user) %> ||</h5>
  <% dash = Dashboard.find_by(:user_id => current_user) %>
  <h5><%= link_to 'View Dashboard', dashboard_path(dash) if @user.dashboards.present? %> ||</h5>
  <% if @user.dashboards.blank? %>
    <h5><%= link_to 'Create New Dashboard', new_dashboard_path %></h5>
  <% end %>    
</br>
<% else %>
  <div class="jumbotron overlay">
    <h2>Welcome to Swiftads App!</h2>
       <p class="lead", style = "color:white">
        ( Not a Smart App Yet:), that generates new customers for your business 
      </p>
     <!-- <p>
        With a few simple clicks you can start advertising your business on Google. 
      </br>
    </br>We do everything for you so you can focus on what's most important...
        running your business.
      </p> -->
  </div>
  <div class="row">
    <div class="plans clearfix">
      <div class="col-lg-3 col-lg-offset-3 plan">
        <h2>SwiftAds</h2>
        <div class="features">
          <ul>
            <li>Algorithm that identifies quality customers to your business</li>
          </ul>
        </div>
        <div class="price pull-right">
           : $300 Per month
          </div>
          <p>
            <%= link_to 'Sign Up', new_user_registration_path, class: 'btn btn-primary sign-up' %>
          </p>
      </div>
      <div class="col-lg-4 plan">
      
      <p class="lead">
        With a few simple clicks you can start advertising your business on Google. 
      </br>
    </br>We do everything for you so you can focus on what's most important...
        running your business.
      </p>
        <!-- <h2>Primuim Pack</h2>
          <div class="price pull-right">
            SwiftAds: $450
          </div>
        <div class="features">
          <ul>
            <li>Unlimited Image Uploads</li>
            <li>Responsive design</li>
            <li>Access anywhere</li>
            <li class='extra'>Unlimited projects</li>
          </ul>
        </div>
          <p>
            <%= link_to 'Sign Up', new_user_registration_path, class: 'btn btn-primary sign-up' %>
          </p> -->
      </div>
    </div>
  </div>
<% end %>